<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <title>{% block title %}Editionista.com - Symfony2 in every flavour{% endblock %}</title>
        <meta name="description" content="Discover and share Symfony2 editions created by the community.">
        <meta name="viewport" content="width=device-width">

        <link rel="stylesheet" href="{{ asset('bundles/editionistawebsite/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/editionistawebsite/css/bootstrap-responsive.min.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/editionistawebsite/css/font-awesome.min.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/editionistawebsite/css/bootstrap-tagmanager.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/editionistawebsite/css/main.css') }}">
        <style>
            body {
                padding-top: 5%;
            }
            
            .space {
                margin-top: 3%;
            }
            
            .muted a {
                color: #6B6B6B;
                font-weight: bold;
            }
            
            .fixed {
                width: 78px !important;
            }
            
            ul.breadcrumb  {
                    background-color: rgba(50, 50, 50, 0.1);                
            }
            
            .packages {
                min-width: 200px;
            }
            
            .well-hover:hover {
                background-color: #f0f0f0;
                border-color: #d1d1d1;
            }
            
            .well .icon-muted {
                color: #ababab;
            }
            
            .icon-muted-hover {
                text-decoration: none;
            }
            
            .icon-muted-hover:hover {
                color: #666;
            }
            
            .alert a {
                color: #C09853;
                font-weight: bold;
            }
            
            .breadcrumb a {
                text-decoration: none;
            }
        </style>
        
        <script src="{{ asset('bundles/editionistawebsite/js/vendor/modernizr-2.6.2-respond-1.1.0.min.js') }}"></script>
    </head>
    <body>
        <div class="navbar navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </a>
                    <a class="brand" href="{{ path('home') }}">Editionista</a>
                    {{ knp_menu_render('main', {'nav_type': 'navbar'}) }}
                    {{ knp_menu_render('right', {'nav_type': 'navbar', 'pull': 'right'}) }}
               </div>
            </div>
            <ul class="breadcrumb">
              {% block breadcrumbs %}
                <li class="active"><i class="icon-home"></i> Home</li>
              {% endblock %}
            </ul>
        </div>
        
        <div class="container">
            {% block alert %}<div class="alert span8 offset2">Editionista is still in a very early stage, things will change and might break. <a href="https://github.com/fabstei/editionista/issues">Feedback</a> and contributions are very welcome!</div>{% endblock %}
            {% block body %}{% endblock %}
                
            {% if dump is not defined %}
                {% set dump = null %}
            {% else %}
                <div class="row-fluid space">
                    <p>{{ dump(dump) }}</p>
                </div>
            {% endif %}
                
            <footer>
                <p class="muted"><a href="http://github.com/fabstei/editionista">This website</a> is built using <a href="http://symfony.com">Symfony2</a>, <a href="http://getbootstrap.com">Bootstrap</a> and <a href="http://www.initializr.com/">Initializr</a>.</p>
            </footer>
        </div>

        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
        <script>window.jQuery || document.write('<script src="{{ asset('bundles/editionistawebsite/js/vendor/jquery-1.8.3.min.js') }}"><\/script>')</script>
        <script src="{{ asset('bundles/editionistawebsite/js/vendor/bootstrap.min.js') }}"></script>
        <script src="{{ asset('bundles/editionistawebsite/js/vendor/bootstrap-tagmanager.js') }}"></script>
        <script src="{{ asset('bundles/editionistawebsite/js/main.js') }}"></script>

        <script>
            {% if not is_granted('ROLE_ADMIN') %}
            var _gaq=[['_setAccount','{{ google_analytics }}'],['_trackPageview']];
            (function(d,t){var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
            g.src=('https:'==location.protocol?'//ssl':'//www')+'.google-analytics.com/ga.js';
            s.parentNode.insertBefore(g,s)}(document,'script'));
            {% endif %}
                
            $(document).ready(function() {
                $("[rel='tooltip']").tooltip();
                
                    $('#tabs a').click(function (e) {
                        e.preventDefault();
                        $(this).tab('show');
                    })
            });
                        
            //$('.fix-margin div:nth-child(odd)').css('margin-left','0');
         </script>
         {% block javascript %}{% endblock %}
    </body>
</html>